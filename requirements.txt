# Unified release requirements.
#
# This single file covers TabCausal, the bundled data engine, and the baseline
# runners.

numpy>=1.23,<2
torch>=2.1
scikit-learn>=1.2
tqdm>=4.66
pandas>=1.5
matplotlib>=3.7
pyarrow==10.0.1

# Data-engine helpers.
pyyaml>=6.0
python-igraph>=0.10

# Baseline dependencies.
causal-learn>=0.1.3
causaldag>=0.1a163
cdt>=0.6.0
rpy2>=3.5
networkx>=3.0
pulp>=2.7

# Keep numba/llvmlite on wheel-backed versions for Python 3.10.
# Newer unconstrained numba releases may trigger a local llvmlite/LLVM build.
numba==0.60.0
llvmlite==0.43.0

# Neural baseline dependencies.
pytorch-lightning>=2.0

# Official AVICI baseline runtime.
# The official AVICI source is included under
# baselines/paper_algorithms/avici_official; these packages provide its runtime.
jax>=0.3.17
jaxlib>=0.3.14
dm-haiku>=0.0.8
optax
deepdiff
huggingface-hub
